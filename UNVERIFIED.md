# 미검증 목록 (UNVERIFIED)

`npm test` 이 전부 통과하는 것과 **이 사이트가 실세계에서 아픈 곳 없이 도는지**는 다른 문장이다.
여기는 테스트로 증명되지 않은 채 남은 항목을 적어 두는 곳이다. "문제 없음" 이라고 말하지 않기 위한 파일.

각 항목을 닫으면 아래 표의 **확인** 칸에 커밋 해시나 측정 결과를 남긴다. 삭제하지 않는다 —
"왜 이게 한때 의심스러웠는지" 가 다음 사람에게 남아야 한다.

확인된 것(SRS r4, 규칙 버전, T-스핀, 레이트리밋, 오프라인 강하, 클라/서버 결정론 등)은
[README](README.md) 와 [SECURITY.md](SECURITY.md) 에 근거와 함께 적혀 있다. 여기엔 **없는 것이** 있다.

---

## 표

| # | 항목 | 지금 아는 것 | 왜 모르는가 | 확인 방법 | 안 하면 어떻게 아픈가 | 확인 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **배포 경로 실물 미실행** — systemd 유닛, nginx/Caddy 설정, `Dockerfile` | 파일은 있고 문법은 읽을 수 있다 | 이 머신에 docker 가 없어 **빌드조차** 안 해봤고, Linux 기동도 안 해봤다 | WSL 또는 싼 VPS 에서 §systemd 그대로 설치 → `systemctl status` → `curl localhost:8787/api/health` → nginx 통과 | 배포 당일에 설정 파일과 실제 머신이 다른 언어로 말하기 시작한다 (경로·권한·`ReadWritePaths`·env 파일) | ☐ |
| 2 | **공유 IP(CGNAT/학교/회사) 과잉 차단** | 운영 한도는 IP당 제출 6/분, 동시 대기 2건. 정량 재현됨: 같은 IP 10대 → **4건 429**, 25대 → **19건**, 40대 → **34건** | `loadtest` 는 테스트 모드(한도 200/2000)로 돌기 때문에 이 경로를 지나지 않는다. 한국은 이통사 CGNAT 가 흔해서 공유 IP 가 예외가 아니다 | 운영 한도 그대로 도는 부하 모드를 추가하고(예: `node tools/loadtest.js 200 50 --prod-limits`) 429 비율을 본다 | 정당한 사용자가 "제출이 자꾸 실패한다" 를 경험한다. 자동화 도구를 막는 것과 같은 코드라 눈치채기 어렵다 | ☐ |
| 3 | **상한 크기 판에 대한 악의 부하** | 재시뮬은 1,000틱당 1.0–1.9ms, 4시간 판 상한은 ≈1.6초. 워커 밖에서는 계산하지 않는다 | 부하 테스트는 "정상 범위 판 200개"만 쐈다. 4시간짜리 판 400건이 동시에 들어올 때의 큐 깊이·메모리·대기 시간·`NT_QUEUE_CAP` 동작이 미측정 | `tools/loadtest.js` 에 장시간 판 생성 옵션을 붙여 측정 (기아 방지·티어 2·503 응답까지 함께 확인) | 폭주 시 사이트가 서는지, 발행이 늦어지는 정도인지를 문서의 숫자가 아니라 실제 경험으로 알아야 한다 | ☐ |
| 4 | **검수 운영 주체 없음** | `tools/review.js` 로 플래그 대기열·지표·순위 이력을 볼 수 있고, `--clear/--hide` 로 처리된다 | 상위권 휴먼 오버(느린 봇)는 구조적으로 자동 판정 불가 → 사람만이 최종 방어선인데, 그 사람을 아직 정하지 않았다 | 배포 전 검수 담당(본인 포함)과 주기(주 1회 등)를 정하고, `flagged` 가 쌓였을 때의 알림 경로를 정한다 | "플래그됨" 은 곧 "아무도 안 보는 대기열" 이 된다. 순위판 신뢰는 그때부터 조용히 깎인다 | ☐ |
| 5 | **규칙 동결 시점** | 지금은 `RULES_ID=r4` 이고 공개 리플레이가 0개 → 규칙을 올려도 비용이 없다 | r5 로 올리면 r4 리플레이는 재제출 거절(`rules-version`) 을 넘어, **재생까지** 새 규칙으로 재시뮬되어 조각 놓이는 자리가 달라 보인다. 과거 규칙 엔진 스냅샷(`legacy/r4/`) 은 의도적으로 안 만들어 두었다 | 공개 배포 전에 "이 시점부터 규칙을 바꾸려면 엔진 스냅샷 보존이 필요하다" 를 선언하고, 가능하면 배포 후 첫 한 달은 규칙을 건드리지 않는다 | 링크가 걸려버린 뒤에는 되돌릴 방법이 없다 (README 규칙 버전 섹션) | ☐ |
| 6 | **성능 수치의 재현성** | 같은 기계·같은 코드로 접수 처리량 162 → 107건/초, 발행 75 → 57.7건/초 (≈2배) | 백그라운드·Defender·전원 계획 등 측정 환경을 통제하지 않았다. 그래서 README 는 이제 범위로 적혀 있다 | 배포 대상 머신에서 `npm run load` 를 3회 + 전원 계획/백그라운드 고정한 채 재실측 | 문서의 숫자를 믿고 용량을 계획하면 틀린다. "밀려도 서버는 선다" 는 성질만 믿는다 | ☐ |
| 7 | **PWA 설치 가능 여부** | `manifest.webmanifest` 는 있고 `/favicon.svg` 만 아이콘으로 참조한다. 서비스 워커는 **없다**(의도적) | 설치 프롬프트는 브라우저 요구(대개 PNG 192/512)를 받는다. 실물 설치 시도 안 해봄 | Chrome/Android 에서 실제 설치 → 오프라인에서 열었을 때 어떻게 되는지 확인(지금으론 캐시가 없어 새로 다운로드될 것) | "설치되는 줄 알았는데 안 된다" 또는 "설치됐는데 오프라인이면 새 로드를 못 한다" 의 혼동. 미구현 항목임을 README 가 이미 말한다 | ☐ |
| 8 | **다중 인스턴스 / SQLite 스케일아웃** | WAL 로 단일 프로세스에서 동작 확인(테스트·부하). 파기는 자동 | 두 인스턴스를 띄우면 `runs` 쓰기가 경합한다. LiteFS/Postgres 전환 경로도 미검증 | 2인스턴스로 같은 DB 파일을 공유해 올려보고(로컬 디스크) 오류·중복 발행을 관찰 → 필요 시 전환 계획 수립 | 트래픽이 커진 뒤에야 "스케일아웃이 안 된다" 를 알게 된다. 지금은 알았고, 그때는 모르게 된다 | ☐ |
| 9 | **브라우저 범위** | Edge/Chrome(headless) 에서 자동 검증: 입력→점수, 렌더링, IndexedDB 지문 유지, 폰트, 크로스 런타임 해시 | Safari/Firefox, 그리고 **실제 모바일 기기**를 한 번도 안 태웠다. 특히 `extractable:false` 키의 IndexedDB 저장, `document.fonts`, 자동재생 정책(음성) 의 구현차 | iOS Safari / Android Chrome / Firefox 에서 실제 플레이 → 새로고침 후 지문 유지 → 제출 → 공유 링크 재생 | 특정 브라우저에서 제출이 조용히 안 되거나, 서명 키가 매번 새로 만들어져 기록 소유가 깨질 수 있다 | ☐ |
| 10 | **실제 HTTPS 환경** | `file://` 에서 `isSecureContext=true`·WebCrypto OK 를 실측. 리버스 프록시 설정 초안은 있음 | 실 TLS 서명·리다이렉트·HSTS 를 거쳐 본 적 없음. 비-HTTPS면 브라우저가 WebCrypto 를 못 써 제출이 꺼진다는 게 **문서상 예측** | 배포 후 `https://` 에서 `/api/health`, 세션 발급, 서명 제출, `/r/` 재생까지 한 바퀴 | HTTPS 없으면 "게임만 되는 사이트" 가 된다. 경고는 DEPLOY 에 있으나 실측이 없다 | ☐ |
| 11 | **OG 미리보기 실물** | `/r/*` 에 `X-Robots-Tag: noindex`, OG 태그, no-referrer 를 붙이고 HTML 생성을 테스트로 확인 | 카카오톡/디스코드/슬랙의 크롤러가 실제로 무엇을 표시하는지 eyeball 확인 안 함 | 실 배포 후 링크를 세 서비스에 던져보고 미리보기를 확인 (이름 노출 여부 포함 — 정책상 이름은 공유자만 보는 페이지에 있다) | 의도와 다른 미리보기(이름 노출/이미지 없음) 가 남에게 먼저 보인다 | ☐ |
| 12 | **기간 집계 경계** | 주 키는 ISO 주(목요일 기준) · UTC, 일/월도 UTC 기준. 라이브/스냅샬 이원화됨 | DST 도 없고 UTC 라 안전해 보이지만, 사용자 체감 "이번 주" 경계(한국 시간 목요일 새벽 등) 를 확인하지 않았다 | 한국 시간대 사용자로 금요일/일요일에 제출해 "주간" 에 잡히는지 확인 | 주말에 세운 기록이 다음 주로 밀려 표시될 수 있다 (기록·순위 자체는 그대로 있고 라벨만 어긋난다) | ☐ |
| 13 | **파기 잡동작** | `pruneIps`(IP 해시 30일 후 NULL)·`sweepTokens`·rate 파기가 코드에 있고 호출을 테스트로 확인 | 10분/1시간 주기가 며칠 도는 실운영에서 어떻게 작동하는지 관찰하지 않았다. 특히 rate 파기 기준을 `seen_at` 으로 바꾼 직후 | 배포 후 로그/통계로 파기 결과 건수를 관찰(`totals` API·DB 카운트) | IP 파기가 너무 잦으면 레이트리밋이 무력화되고(이미 한 번 겪은 부류), 너무 드물면 PII 보유가 길어진다 | ☐ |
| 14 | **닉네임 moderation(한국어)** | 금칙어 필터는 라틴어권만 걸린다. 한국어는 필터가 오히려 위험하다고 판단해 "이름은 공유 링크에만 보인다" 로 대체했다 | 실제 사용자가 악의적 이름을 냈을 때 링크가 퍼진 경우의 대응(운영이 `tools/review.js --hide` 로 목록 제외) 가 문서에만 있고 실제로 해본 적이 없다 | 배포 전 `--hide` 로 목록 제외 → 데이터는 남는지 확인하는 한 바퀴를 실제로 돌려본다 | 대응 절차를 모르면 첫 사고에서 아무도 못 한다 | ☐ |

---

| 15 | **Windows 를 "상시 서버"로 쓰는 경로** (Task 스케줄러 / nssm 서비스화, 방화벽 인바운드, IIS 뒤에 두기) | 서버 자체는 Windows 에서 돈다 — `verifytest`·`e2e`·`loadtest` 가 이 머신에서 같은 `server/server.js` 를 띄워 `/api/health`·`/api/board` 응답을 매번 확인하고, `QUICKSTART.md` §B 의 포그라운드 실행도 같은 경로다 | "켜져 있다"와 "끄고 켜도 알아서 뜨고 남에게 보인다"는 다른 문장이다. 서비스 등록·재시작 정책·방화벽·IIS ARR 은 한 번도 돌려보지 않았다 | 이 머신에서 ① Task 스케줄러 부팅 시 실행 ② 방화벽 인바운드 8787 ③ 다른 기기에서 접속 — 을 실제로 해보고 결과를 여기에 남김 | Windows 사용자는 "빨리 시작은 됐는데 재부팅하면 사라진다" 를 문서 없이 겪게 된다 | ☐ |

## 싸게 끝나는 순서 (추천)

1. **WSL/저가 VPS 에서 `DEPLOY.md` 를 글자대로 실행** (#1) — 반나절이고, 배포 당일의 대부분 사고를 미리 소모한다.
2. **운영 한도로 도는 부하 모드**(#2 #3) — 코드 소량, 공유 IP 결론을 numbers 로 얻는다.
3. **실 기기 3종 스모크**(#9) — Safari 만이라도.
4. 규칙 동결 선언(#5) → 초기 배포 → 그 다음에 PWA/스케일아웃(#7 #8) 을 별도 결정으로.

## 이 파일의 사용법

- 새 미검증 항목이 보이면 여기 **추가**하고, 코드/문서에서 고치지 말고 여기 있는 것을 먼저 알린다.
- 닫히면 표의 확인 칸에 근거(커밋 해시, 측정 로그 경로) 를 남긴다.
- `npm test` 가 통과한다고 이 파일이 짧아지지는 않는다. 서로 다른 일을 한다.
