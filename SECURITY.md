# 위협 모델 (무엇을 막고 무엇을 못 막나)

목표는 두 개다.

1. **가짜 기록이 월드 레코드를 더럽히지 못하게** 한다.
2. **기록과 사람이 결부되지 않게** 한다 — 보드를 보는 사람 이름이 드러나면 안 된다(보복·스토킹).

## 원칙: **작성자가 아니라 주장을 검증한다**

서버에 남아 있는 유일한 진실은 둘뿐이다: ① **이 입력 스트림이 이 결과를 낸다**(재시뮬) ② **그 판을 치는 데 최소 이만큼의 시간이 걸렸다**(월클럭). 나머지는 전부 클라이언트 신고다.

그래서 **사람처럼 쳐진 기계**는 원리적으로 구분하지 못 한다. 그 정보(누가, 어떤 손으로, 무슨 도구로) 가 서버에 한 번도 도착하지 않기 때문이다. 그래서 이 프로젝트는 그 시도를 하지 않는다 — 자동 판정은 "말도 안 되는 수치"만 잡는다.

그리고 이건 실속이 꽤 크다: 점수·줄·조각·틱·보드해시를 **하나라도 어긋나면 죽게** 다시 계산하므로, 내부적으로 모순된 수치는 재시뮬이 전부 삼킨다("12조각으로 40줄" 주장 → 재시뮬은 12조각 결과를 내므로 불일치 기각). 남는 문제는 모순 없는 스트림의 **작성자**이고, 그건 못 잡는 것이 아니라 **알 수 없는 것이다**.

## 막는 것 (자동)

| 공격 | 방어 | 근거 코드 |
| --- | --- | --- |
| 점수/줄/조각/틱/보드해시 위조 | 서버가 리플레이를 **다시 돌려** 전부 일치해야 발행 | `server/verify.js:verify()` |
| 게임 시간 조작(2배속 클레임) | 재시뮬 틱이 클레임과 달라짐 + 월클럭 하한 | `verify()` 4 |
| 남이 만든/과거의 시드 재사용 | 시드는 서버가 발급, 1회용(원자적 소진) | `db.js:consumeToken()` |
| 유리한 시드 골라쓰기 | 시드는 서버가 HMAC 비밀로 발급, 클라이언트 선택 불가 | `server.js /api/session` |
| 남의 리플레이 복사→내 이름 등록 | ① 시드 1회용(그대로 재제출 불가) ② 내용 digest UNIQUE(선등록 우선, 409 + 원본 링크) ③ 소유 서명 | `verify.js:digestOf`, `server.js /api/submit` |
| 내 서명을 남의 내용에 붙이기 | 서명 대상 = `NTSUB1\n<content digest>\n<nonce>` | `identity.js:authPayload` |
| 지문(`bbewsh`) 총공격으로 남의 기록 열거 | `/api/mine`, `/api/hide`는 서명 요구 | `server.js:ownership()` |
| 공유 링크 무작위 추측으로 남의 기록 뒤지기 | 링크 키 **64bit + 체크섬**, 만료 없음·검색 인덱스 없음. 맞혀도 공개 정책상 이름은 안 나옴 | `db.js:shareKey()` |
| 이름으로 모든 기록 뒤지기 | **그 API가 존재하지 않는다.** 이름은 공유 링크에만 표시 | `verify.js:publicRun` |
| 보드 응답에 이름 슬쩍 | 보드/기간/집계 뷰는 `displayName` 필드를 만들지 않음(테스트로 고정) | `tools/verifytest.js` '이름 비노출 정책' |
| 검색엔진이 공유 페이지 이름 색인 | `/r/*`에 `X-Robots-Tag: noindex` | `server.js:serveIndex` |
| 이름 칸에 링크/스크립트/RTL 오버라이드 | 길이·문자·링크·bidirectional 금지 + NFC 정규화 + 표시는 `esc()`/`textContent`, CSP `script-src 'self'` | `verify.js:cleanDisplayName`, `server.js` 헤더 |
| 내부 파일(`server/db.js`, `data/tetris.db`) 노출 | 정적 파일 허용 목록 | `server.js:STATIC` |
| 부하 공격(재시뮬은 CPU를 쓴다) | 본문 512KB, 입력/틱 상한, **재시뮬은 워커 스레드만**(메인 스레드 정지 불가), 대기열 상한 + 네트워크/지문당 동시 대기 제한, IP·지문별 레이트리밋, 기각 반복 시 자동 차단 | `config.js:LIMITS`, `server/queue.js` |
| 대기열 앞줄 사기(우선순위 어뷰지) | 우선순위에 **주장 점수를 쓰지 않는다**. 검증 이력 + 기각 0 + 상위권 전망이 있는 기기만 우선 티어. 티어 안은 FIFO, 오래 기다리면 승격 | `server/queue.js:tierOf` |
| 다른 규칙 버전 리플레이로 재시뮬을 태워 워커를 묾 | 재시뮬 전에 `rules-version` 으로 early 거절(시뮬은 큐에서 가장 비싼 작업) | `verify.js` 상단 |

**기각 사유는 5종으로 좁혀 있다**: 형태 / 시드 / digest 소유자 불일치 / 재시뮬 불일치 / 월클럭. 나머지는 **통과**시킨다.

속도·간격·반응 지표(PPS, APM, 동작간격 최빈, 반응 중앙값…) 는 계산해서 행에 **기록만** 하고 판정에는 쓰지 않는다. 이유는 단순하다: 세계 상위권은 CTWL·ASD·롤킹 같은 손기술과 전용 자세로 "인간 한계"로 여겨지던 값들을 그냥 넘기고, 반대로 자동화된 도구는 사람 속도로 느리게 돌려 같은 문턱을 피한다 — 즉 그 지표로 자르면 **정당한 최상위를 벌하고 도구는 놓친다**. 자동 판정은 "서버가 같은 입력으로 같은 결과를 재구성하는가" 하나만 쓴다.

`⚑(flagged)` 는 사람이 `tools/review.js --flag` 로 붙인다. 자동 판정으로 사람을 못 들어가게 하는 것은 승인이 필요한 보드에서 특히 나쁜 기본값이기 때문이고, 무엇보다 **만능 자동 탐지는 존재하지 않는다.**

## 시간 기반 어뷰지 — 틱과 실제 시간이 어긋나는 문제

순위 지표가 **틱**으로 매겨진다(스프린트 = 40줄까지의 틱, 울트 = 2:00 예산). 그런데 클라이언트는 프레임이 250ms 를 넘으면 그 초과분을 버린다(`game.js` 의 dt 클램프 — 스파이럴 방지로는 표준). 결과: **게임 시계는 실제 시간보다 느리게만 간다**. 그래서 "일부러 느리게 굴리기"(백그라운드 스로틀, CPU 점유, devtools throttle) 를 하면 같은 실력으로 **더 짧은 틱 기록**이 나오고, 틱당 실제 밀리초가 늘어나므로 오히려 **쉬워진다**. 실익이 두 번 생긴다. 서버의 현재 검사는 반대 방향 하나뿐이다:

```
시뮬 시간 > 제출까지의 실제 경과 + 유예   →  기각 (wallclock)
```

즉 "실제보다 짧게" 는 조용히 통과한다. 이게 알려진 구멍의 정확한 모양이다.

**왜 지금 막지 않는가** — 막는 세 가지 방법과 그 비용:

| 방법 | 되는 일 | 안 되는 일 / 비용 |
| --- | --- | --- |
| 순위를 실제 ms 로 매긴다 | 측정은 간단하다 | 순위 근거가 **클라이언트 신고**가 된다. 서버가 혼자 아는 시간은 `시드 발급→제출` 총량뿐이고 이건 상한이라 "더 빨랐다"를 증명하지 못한다 |
| 실시간 고정 시계(틱 = elapsed×60, 밀린 틱을 되돌리지 않고 소진) | 밀어서 이득이 사라진다(밀면 죽는다) | 정지·탭 스위칭은 여전히 실시간을 숨긴다. 게임 경험도 딱딱해진다 |
| **서명된 타임스탬프 비콘** | 위조 불가능한 유일한 방법: 서버가 게임 중 2초마다 일련 번호를 주고, 클라이언트를 거쳐 입력 스트림에 실리면 서버가 (비콘, 틱) 을 자기 시간과 대조해 틱/실시간 비율이 1에서 벗어나면 기각 | 게임 중 폴링 0.5 req/s, 엔드포인트·비콘 테이블 추가. (시뮬 규칙과 무관한 메타라서 `RULES_ID` bump 는 필요 없다) |

지금은 이렇게 해 두었다(계측 추가 없음 — `game.js`·`engine.js`·리플레이 포맷 그대로, `RULES_ID` 그대로):

| 값 | 무엇 | 어디에 |
| --- | --- | --- |
| **재현 시간** `sim_ms(=time.simMs)` | 서버가 같은 입력으로 다시 돌려본 시간. **순위는 이쪽으로만 매겨진다** | `runs.ticks` 에서 결정적으로 계산 (컬럼 `ticks`) |
| **클라이언트 경과** `real_ms` | 시드 발급→제출 도착까지 서버가 **혼자 잰** 실측 | 컬럼 `real_ms` (+ `metrics.realMs`) |
| **차이** `time.deltaMs` | 위 둘의 빼기. 따로 계산한 숫자를 두 군데에 실지 않는다 | API/화면 |

**검증된 기록에서는 이 둘을 나란히 보여준다**(`.rp-timing`, 재생 화면과 제출 결과). 순위 표시를 교체하는 것이 아니라 추가 정보다. 기각된 기록에는 보여주지 않는다.

이 대비가 **증명하는 것과 못 하는 것**: `재현 > 실측 + 유예` 는 물리 성립 불능이라 서버가 기각한다(기존 wallclock). 반대쪽 `실측 ≫ 재현` 은 어뷰지(밀린 프레임)일 수도 있고 게임 오버 화면에 앉혀 둔 것일 수도 있다 — 그래서 **자동 판정하지 않고** 검수자가 본다(`tools/review.js --show` 의 `실시간배율`). 완전한 봉인은 서버가 게임 중 타임스탬프를 발급하는 방법뿐이다.

> 이 항목을 만드는 동안 고장 두 개가 나왔다: ① `metrics.realMs` 를 넣은 직후 `out.metrics = m` 이 통째로 덮어써서 실측치가 **한 번도 DB 에 저장된 적이 없었다**(검수의 배율이 항상 null 이던 이유) ② 컬럼 `sim_ms` 는 **재시뮬에 걸린 시간(비용)** 이라 게임 시간과 무관한데 이름이 헷갈렸다. 지금은 각각 회귀 검정으로 고정(`verifytest` 「재현 시간 / 클라이언트 경과」).

## 레이트리밋은 어떻게 세나

`db.takeSlot()` 은 **가중 이동 창**을 쓴다 — `지금 창 사용량 + 바로 앞 창 사용량 × (지금 창이 아직 안 지난 비율)`.

왜 그냥 창을 나눠서 리셋하지 않나 (r 전까진 그랬다):

| 구멍 | 무슨 일이 있었나 | 재현 |
| --- | --- | --- |
| **경계 버스트** | 창이 바뀌는 순간 카운터가 0 으로 돌아간다. 59.9초에 한도를 채우고 0.1초에 다시 채우면 **2배**가 통과한다. | 실제 `takeSlot` 에 1초 창을 넣어 증명: 한도 6/초를 1.41초에 **18건** 통과 |
| **파기가 시간 창을 지움** | 파기 조건이 `window_at < (분 단위 눈금)` 이었는데, 1시간 창 레코드의 `window_at` 은 눈금 단위가 60배 작아서 **매번** 삭제 대상이었다. 10분마다 도는 파기 → "시간당 30" 이 실질 "10분당 30" (=180/시간) 으로 부풀어 있었다. | 5개 사용한 뒤 `pruneIps()` → 나머지 30개가 전부 통과하는 것으로 확인 |

같이 고친 것: 파기는 `window_at` 이 아니라 **마지막 접근 시각(`seen_at`)** 기준으로 한다. 창 종류와 무관하게 "오래 안 쓰인 키"만 지워진다.

이건 테스트 플레이크 문제이기도 하다: 경계에서 카운터가 열리면 "한도를 넘기면 거절당한다"는 검증이 우연히 통과 버전을 타버린다.

## 못 막는 것 (정직하게)

- **폭주는 결국 지연이다**: 위 실측으로도 제출 폭주 시 평균 대기 1~1.5초가 생긴다. 사이트는 서지 않지만 기록 발행은 늦어진다. 급격히 밀면 `NT_WORKERS` 를 올리고,그래도 밀면 유입(`NT_QUEUE_CAP`, `NT_INFLIGHT_IP`)을 깎는다.
- **느린 봇 / 어시스트**: 사람이 허용 범위 안에서 입력을 자동화하면 구분할 방법이 없다(위 원칙). 지표는 계산해서 행에 남기지만 **경고로 쓰지 않는다** — `⚑` 는 사람이 리플레이를 보고 붙인다(`tools/review.js --flag`). 실질 방어선은 "리플레이가 늘 공개되어 있다"는 사실과, 기록이 지워지지 않는다는 점(나중에 번복해도 이력이 남는다)이다.
- **같은 사람의 여러 지문(스머핑)**: 새 브라우저 = 새 지문. IP 해시로 같은 네트워크임을 보여줄 뿐, 신원이 아니므로 차단 근거로 쓰지 않는다. 상위권에 계정 요구를 추가하려면 `owners` 에 OAuth 바인딩을 붙이는 방식이 최소 변경 경로다.
- **렌더 압축**: 프레임이 밀리면 게임 시간이 실제보다 느리게 간다 → 스프린트·울트에서 기록이 실제보다 짧아질 수 있다. 정직한 플레이를 벌하지 않으려면 속도 쪽을 기각도 플래그도 하지 않는 것이 맞다(그래서 그렇게 되어 있고, 대신 재현/실측 **두 값을 다 남긴다**). 봉인은 서버 발급 타임스탬프뿐이고 그건 필요해질 때의 이야기로 미뤘다.
- **IP는 신원이 아니다**: 같은 공유기 = 여러 사람, CGNAT/IPv6 = 수시로 변경. 동명이인 구분의 **보조** 신호로만 쓰고 화면에는 앞 두 자리만 보여준다.
- **닉네임 검열은 한국어에 약하다**: 목록 필터는 자모 분리·오타 변형을 못 잡는다(넥슨도 이 이유로 자음/모음 분석을 쓴다). 대신 "이름은 공유 링크에만" 규칙으로 문제를 없앴다. 금칙어 필터는 라틴어권 명백한 것만 걸러 OG 카드에 그대로 나가는 일을 줄인다.
- **키 유실**: IndexedDB를 지우거나 다른 기기로 가면 내 기록 링크로 돌아온다(서버에 소유자 인덱스를 만들지 않는 대가). 공유 링크가 곧 지갑이다.
- **링크 재공유**: 본인이 보낸 링크가 퍼지면 이름도 퍼진다. 링크 만료(`?exp=`)는 아직 없다. 그래서 제출 화면에서 **만들기 전에** 이 사실을 한 줄로 보여 준다(`submit.nameWarn`).
- **실력 지문**: 닉네임이 없어도 플레이 스타일(20G 처리, T-스핀 습관)로 사람을 알아보는 사람은 있다. 비용은 올라가지만 불가능해지진 않는다.

## 개인정보

| 데이터 | 저장 | 보관 |
| --- | --- | --- |
| 닉네임 | `owners.display_name` (문자열, 보드 응답에 없음) | 무기한(삭제 요청 시 `recordNote`/숨김 + 필요하면 열 제거) |
| 접속 IP | 원문 저장 **안 함**, `HMAC(ip + 일별 도메인)` 앞 24 hex | 30일 후 NULL 파기(`db.pruneIps`) |
| 공개용 IP 힌트 | `runs.ip_hint` (예: `112.164.xx.xx`) | 기록과 함께 |
| 기기 지문 | 공개키 해시 base32 5 + 체크섬 1 | 무기한 (역추적 정보 없음) |
| 개인키 | 서버에 **없음** (클라이언트 IndexedDB, `extractable:false`) | 사용자 브라우저를 나가는 일 없음 |

## 신고/검수 운영

```bash
node tools/review.js            # flagged 대기열
node tools/review.js --show <share>
node tools/review.js --clear <share>   # verified 로 승인
node tools/review.js --flag <share> 메모   # 사람이 리플레이를 보고 ⚑ (여기서만 flagged 가 된다)
node tools/review.js --hide <share>    # 목록 제외(데이터·이력 보존)
```

⚑ 표시된 기록은 보드에 `⚑`로 드러난다. 숨기지 않고 드러내는 편이 어뷰지를 숨기는 것보다 낫다(TAS를 일반 보드에 섞지 않고 별도 분류로 두는 관례와 같은 이유).
